apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - persistentvolumeclaim.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app: portfolio-video

# 이미지 설정은 주석 처리 (로컬 이미지 사용 시)
# 원격 레지스트리 사용 시 아래 주석을 해제하고 실제 레지스트리 주소로 변경하세요
# images:
#   - name: portfolio-video
#     newName: your-registry.com/portfolio-video
#     newTag: latest

