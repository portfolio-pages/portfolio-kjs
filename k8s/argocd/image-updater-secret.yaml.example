# 이 파일은 예시입니다. 실제 GitHub 토큰으로 Secret을 생성하세요.
# 
# 생성 방법:
# kubectl create secret generic argocd-image-updater-secret \
#   --namespace argocd \
#   --from-literal=github_token=<YOUR_GITHUB_TOKEN>
#
# 또는 파일로 생성:
# echo -n "<YOUR_GITHUB_TOKEN>" | kubectl create secret generic argocd-image-updater-secret \
#   --namespace argocd \
#   --from-file=github_token=/dev/stdin

apiVersion: v1
kind: Secret
metadata:
  name: argocd-image-updater-secret
  namespace: argocd
type: Opaque
stringData:
  github_token: "YOUR_GITHUB_PERSONAL_ACCESS_TOKEN_HERE"
  # GHCR 인증을 위한 추가 설정 (필요시)
  ghcr: |
    {
      "auths": {
        "ghcr.io": {
          "auth": "BASE64_ENCODED_USERNAME:TOKEN"
        }
      }
    }

