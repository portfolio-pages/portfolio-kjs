apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - persistentvolumeclaim.yaml
  - configmap.yaml
  - deployment.local.yaml  # 로컬 배포용 deployment 사용
  - service.yaml
  # ingress.yaml은 선택사항

commonLabels:
  app: portfolio-video

# 로컬 이미지 사용 시 images 섹션 불필요
# 이미지는 deployment.local.yaml에서 직접 지정됨

